/*
package com.company;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class nextPrevButtons implements ActionListener {
    JButton nextButton;
    JButton prevButton;
    public nextPrevButtons(){
        ImageIcon nextImg = new ImageIcon();
        ImageIcon prevImg = new ImageIcon("back_Button.png");
        myframe frame1 = new myframe();
        nextButton = new JButton();
        prevButton = new JButton();
        prevButton.setIcon(prevImg);
        nextButton.setIcon(nextImg);
        nextButton.setBounds(700,400,80,50);
        prevButton.setBounds(600, 400,80,50);

    }


    @Override
    public void actionPerformed(ActionEvent e) {

        if (e.getSource()==nextButton){

        }
        else if (e.getSource()==prevButton)
        {

        }
    }
}
*/
